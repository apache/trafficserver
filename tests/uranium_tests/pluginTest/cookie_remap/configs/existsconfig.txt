# This is a test configuration

# Do a regex against the cookie
op:
  cookie: fpbeta
  operation: exists
  sendto: http://127.0.0.1:{SERVER_HTTP_PORT}/cookieexists
  else: http://127.0.0.1:{SERVER_EXTRA_HTTP_PORT}/cookiedoesntexist
