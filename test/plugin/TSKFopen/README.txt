
INKfopen: returns 0 on error. 

retVal for INKfgets(sourceReadFile,inputBuf,statBuffInputText.st_size)
in success is a pointer to the buffer where data was read into.


* 4.2 INKfopen (create) for write
DOCS: Files does not have to exist. File will be created.

