schema_version: 1
owner: Property Owner
reference: For testing ATS behavior 🍎
rcpt: httpbin
schemes: [http, https]

mappings:
  - alias:
    - httpbin.example.com
    origin: https://httpbin.edge.example.com

rules:
  default:
    priority: background
    origin_host_header: alias
    deny_methods: [ CONNECT ]
