(program (section REMAP { (conditional (ifStatement if (condition (expression (term (factor (comparison (comparable from.url.path) ~ (regex /foo/)))))) (block { (statement inbound.req.From-URL = (value "allowed") ;) }))) }) <EOF>)
