name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: brew install automake openssl; autoreconf -if; ./configure --with-openssl=/usr/local/Cellar/openssl\@1.1/1.1.1j
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
